@model IEnumerable<ProfileSelect.ViewModels.StatusViewModel>
@{
    Layout = "~/Views/Shared/_BaseLayout.cshtml";
    ViewBag.Title = "Статусы";
}

<div class="table-responsive col-md-6">
    <a href="@Url.Action("Add", "Status", new {Area="Admin"})" class="btn btn-primary pull-left margin-bottom-10">Добавить статус</a>
    <table class="table table-bordered">
        <thead>
            <tr>
                <th>#</th>
                <th>Название</th>
                <th>Действия</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.Id</td>
                    <td>@item.Name</td>
                    <td>
                        <div id="navigation">
                            <ul>
                                <li><a href="@Url.Action("Edit", "Status", new {Area="Admin", item.Id})">Редактировать</a></li>
                                <li><a href="@Url.Action("Delete", "Status", new {Area="Admin", item.Id})">Удалить</a></li>
                            </ul>
                        </div>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
