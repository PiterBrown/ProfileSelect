@model IEnumerable<ProfileSelect.ViewModels.DirectionViewModel>
@{
    Layout = "~/Views/Shared/_BaseLayout.cshtml";
    ViewBag.Title = "Специальности";
}

<div class="table-responsive col-md-6">
    <a href="@Url.Action("Add", "Direction", new {Area="Admin"})" class="btn btn-primary pull-left margin-bottom-10">Добавить специальность</a>
    <table class="table table-bordered">
        <thead>
            <tr>
                <th>#</th>
                <th>Код</th>
                <th>Название</th>
                <th>Действия</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.Id</td>
                    <td>@item.Code</td>
                    <td>@item.Name</td>
                    <td>
                        <div id="navigation">
                            <ul>
                                <li><a href="@Url.Action("Edit", "Direction", new {Area="Admin", item.Id})">Редактировать</a></li>
                                <li><a href="@Url.Action("Delete", "Direction", new {Area="Admin", item.Id})">Удалить</a></li>
                            </ul>
                        </div>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>

